# INT-4 failure-drill ledger

Rendered from `scripts/ceremony/int4d-matrix.json`. `cited-ci` means the named case runs continuously; `executed` means the INT-4d disposable drill ran; `deferred` names the prerequisite rather than inventing an unwired seam.

| §11 drill | Source | Detection | Containment | Recovery | Audit refs | Owner |
|---|---|---|---|---|---|---|
| Dispatcher crash before submit | `cited-ci` | Expired generation-1 claim is detected on the next dispatcher cycle, bounded by the configured claim TTL. | The immutable derived run id and generation fence prevent an unfenced retry. | A second holder acquires generation 2 and submits exactly one attempt-1 run. | `takes over after-claim-before-submit with one derived Harness run` ([source](test/integration/int4c-lease-takeover.test.ts)) | dispatcher operator |
| Dispatcher crash after submit before binding write | `cited-ci` | The expired submitted claim is observed on the next dispatcher cycle after its TTL. | The derived run id makes the retry an idempotent lookup rather than a second run. | The contender adopts the attempt-1 run and writes the missing immutable binding. | `takes over after-submit-before-binding by adopting one existing run` ([source](test/integration/int4c-lease-takeover.test.ts)) | dispatcher operator |
| Duplicate webhook/event | `executed` | Repeated and out-of-order reads are compared against durable task and decision state in the same reconciliation batch. | Projection remains monotonic and decision persistence remains idempotent. | Replay converges to one binding, one lifecycle transition, one handoff decision, and no alert. | `replays duplicate and out-of-order events without duplicate control effects` ([source](test/integration/int4d-drill-matrix.test.ts)) | dispatcher operator |
| Harness/DBOS unavailable | `cited-ci` | The independent watchdog probes the Harness Postgres boundary each cycle. | The watchdog remains outside the dispatcher and Hermes failure domains. | Operator restores the Harness data source; forwarding resumes without dispatcher mutation. | `alerts on either unreachable Postgres boundary without stopping itself` ([source](test/unit/harness-watchdog.test.ts)) | watchdog |
| Harness worker killed mid-run | `executed` | A durable capability completion is observed before SIGKILL and the watchdog reports the source gap on its next thresholded cycle. | DBOS resumes the same run and the Harness event store must contain one CapabilityCompleted row per invocation id. | A replacement worker drives the original run to a terminal outcome. | `kills and restarts the pinned Harness worker without duplicating a protected effect` ([source](test/integration/int4d-drill-matrix.test.ts)) | kernel |
| Board unavailable | `executed` | The dispatcher advances durable state while no board consumer is running; the board replay is fingerprinted against every control table. | The board path has read-only dependencies and no control authority. | Restarted projection reads the terminal task and performs zero store writes. | `keeps execution durable through board outage and replays with zero writes` ([source](test/integration/int4d-drill-matrix.test.ts)) | operator-ceremony |
| Policy changes after approval | `executed` | The active version/hash identity is compared before submit and on each reconciliation of a bound run. | Pre-submit work is blocked; bound immutable work is warned and never cancelled. | Operator reviews the visible drift status and proposes a new approved task version when required. | `refuses pre-submit policy drift and flags a bound run without cancelling` ([source](test/integration/int4d-drill-matrix.test.ts)) | dispatcher operator |
| Approval/task hash mismatch | `cited-ci` | The approval payload hash is recomputed before any claim or submit. | The task is blocked before Harness receives authority. | Operator creates and approves a correct immutable task version. | `refuses an approval-hash mismatch before claim or submit` ([source](test/integration/int2-automated-suite.test.ts)) | dispatcher operator |
| Capability expansion | `cited-ci` | Typed attenuation compares the requested grants with the pinned profile before submit. | Any undeclared or delegating capability blocks dispatch. | Operator narrows the task/profile and approves a new version. | `accepts a seven-capability profile with strictly narrower authority` ([source](test/integration/int2-automated-suite.test.ts))<br>`rejects memory.propose in the outer production profile loader` ([source](test/integration/int2-automated-suite.test.ts)) | dispatcher operator |
| Budget/deadline exhaustion | `cited-ci` | Projection compares measured elapsed time, tokens, and calls with the approved limits every reconciliation. | Live overruns are cancelled; already-terminal verified work preserves its truthful result with an overrun warning. | Operator reviews the named dimension and decides whether a new task version is justified. | `preserves a verified terminal handoff after a measured token overrun` ([source](test/integration/int2-automated-suite.test.ts))<br>`D4: cancels and fails a live budget-exhausted run with one alert` ([source](test/unit/reconcile-run.test.ts)) | dispatcher operator |
| Global HALT during run | `cited-ci` | HALT is checked before dispatch and before each active-run reconciliation. | The live run is cancelled and no handoff is created. | Only the operator removes HALT and authorizes later work. | `HALT cancels a bound live run and never creates a handoff` ([source](test/integration/int2-automated-suite.test.ts)) | operator-ceremony |
| Verifier outage | `deferred` | The current deterministic verifier is in-process with the worker, so no separately unavailable verifier boundary exists to detect.<br>Deferred 2026-08-06: A separate verifier outage cannot be injected honestly because the deterministic verifier runs in-process with the worker. The existing invalid-handoff refusal proves containment only; it does not prove detection or recovery for a boundary that is not wired. Prerequisite: **rubric review / separate LLM-judge packet**. | Missing or invalid verification already prevents a verified handoff, but that is not an outage drill. | The future separately wired judge must define retry and operator escalation without treating absence as acceptance. | `marks completed-but-unverified work failed without a handoff` ([source](test/unit/reconcile-run.test.ts)) | operator-ceremony |
| Verification timeout | `cited-ci` | Bounded command execution converts a missed verifier deadline into a typed timeout. | The timeout remains a retryable failure and never becomes acceptance. | The operator reviews the timed-out run before any new task version. | `preserves retryable timeout failures` ([source](test/unit/harness-control-port.test.ts)) | kernel |
| Verifier rejects output | `cited-ci` | The independent verification report records required failed criteria. | The task fails with zero handoff decisions. | Operator inspects evidence and decides whether to approve a new task version. | `rejects the negative fixture on its merits with no handoff` ([source](test/integration/int2-automated-suite.test.ts)) | operator-ceremony |
| Malicious/oversized event | `executed` | The bounded Harness read port rejects oversized output and stable poison fingerprints reach quarantine at the configured cycle threshold. | The binding is durably quarantined and secret-shaped content is absent from alerts and pilot status. | The watchdog and read-only pilot remain available while the operator inspects and explicitly clears the marker. | `quarantines oversized secret-shaped output without leaking to sinks` ([source](test/integration/int4d-drill-matrix.test.ts)) | watchdog |
| Conflicting run binding | `cited-ci` | Dispatcher and independent watchdog derive the intended id and compare it with durable bindings. | The work item receives a durable quarantine marker and critical alert. | Operator resolves the binding and explicitly unquarantines the task. | `marks a conflicting binding while the independent watchdog alerts` ([source](test/integration/int4b-quarantine.test.ts)) | watchdog |
| PR head changes after handoff | `cited-ci` | The sender rechecks live base/head identity immediately before actuation. | Drift refuses instead of rebasing or sending a mismatched payload. | Operator rebuilds a new payload from a newly approved task when appropriate. | `refuses base drift introduced after payload construction` ([source](test/integration/int3b-payload-sender.test.ts)) | actuation operator |
| PR API failure | `cited-ci` | Remote state is queried by derived head before every create/retry. | A crash after create can leave at most one PR. | Retry adopts the existing exact payload and converges. | `converges after a crash following remote creation` ([source](test/integration/int3b-payload-sender.test.ts)) | actuation operator |
| Averray control-plane outage | `deferred` | No Averray control-plane call exists on the supervised Harness dispatch path today, so there is no live boundary to make unavailable.<br>Deferred 2026-08-06: An outage drill would be fake while Harness dispatch makes no Averray control-plane call. The future packet that introduces that authority inherits the disposable outage drill before the seam can be enabled. Prerequisite: **economic-seam integration packet**. | The current path cannot mutate the absent boundary; that absence is not outage containment evidence. | The packet that wires the economic control-plane seam must add bounded detection, refusal, and operator recovery. | None — unwired seam | operator-ceremony |
| Settlement replay | `deferred` | Settlement is not wired to Harness dispatch, so no integration replay can currently be observed here.<br>Deferred 2026-08-06: The integration has no settlement submission path to replay. External idempotency evidence does not prove this repository's future binding, so the wiring packet inherits an exactly-once replay drill. Prerequisite: **economic-seam integration packet**. | Averray-side idempotency mechanisms exist outside this integration, but they are context rather than this drill's evidence. | The economic-seam packet must replay the same settlement intent against the real integrated store and prove one effect. | [source](docs/HARNESS_INT4_CHARTER.md) | operator-ceremony |
| Direct fallback after Harness failure | `deferred` | Direct fallback is an operator policy decision and no automatic fallback seam is enabled.<br>Deferred 2026-08-06: There is deliberately no automatic direct fallback to inject. A drill before the operator's INT-5 decision would invent authority that the system does not possess. Prerequisite: **INT-5 fallback-retirement ceremony**. | The current system fails closed instead of silently expanding authority to a direct agent. | INT-5's fallback-retirement ceremony must decide and evidence any explicit successor path. | None — unwired seam | operator-ceremony |
| Hermes unavailable | `cited-ci` | The standalone watchdog continues source probes and direct forwarding while Hermes-facing containers are absent. | No watchdog import or credential depends on Hermes or slack-operator. | Hermes may restart independently; watchdog alert delivery never waits for it. | `has no slack-operator or Hermes-facing import` ([source](test/unit/harness-watchdog.test.ts))<br>[source](docs/evidence/INT4A_WATCHDOG_DRILLS_2026-08-05.md) | watchdog |
